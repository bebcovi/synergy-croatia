.project_short

  %h2.title= project.title
  = project.summary

  %dl
    %dt= t(".location")
    %dd.location= project.location

    %dt= t(".duration")
    %dd.duration= project.duration

    %dt= t(".attachments")
    %dd.attachments!= project.attachments.map { |a| link_to(a.name, a.file.url) }.join("<br>")
