= simple_form_for [:admin, @category], defaults: {wrapper_tag: :li} do |f|

  %fieldset.inputs
    %ol
      = f.input :name_en
      = f.input :name_hr

  %fieldset.actions
    %ol
      %li= f.button :submit
      %li.cancel= link_to "Cancel", collection_path

