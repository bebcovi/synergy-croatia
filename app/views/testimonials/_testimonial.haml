- present testimonial do |testimonial_presenter|

  - if admin?
    .controls-small
      = testimonial_presenter.edit_button "Izmjeni"
      = testimonial_presenter.delete_button "Izbriši"

  %blockquote
    = testimonial_presenter.content
    %p
      %cite
        %strong #{testimonial_presenter.author},
        #{testimonial_presenter.project}
