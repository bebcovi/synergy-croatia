- present testimonial do |testimonial_presenter|

  %article{class: ("fade_out" if admin?)}
    - if admin?
      .controls
        = testimonial_presenter.edit_button "", title: "Izmjeni"
        = testimonial_presenter.delete_button "", title: "Izbriši"

    %blockquote
      = testimonial_presenter.content

      %p
        %cite
          %strong #{testimonial_presenter.author},
          #{testimonial_presenter.project}
