%header.site_header
  .container
    .site_logo= link_to image_tag("logo.png", alt: "logo"), root_path

    %ol.site_lang
      %li= link_to image_tag("lang/Croatia.png", size: "32x32"), locale: :hr
      %li= link_to image_tag("lang/United-Kindom.png", size: "32x32"), locale: :en

    - if logged_in?
      .logged_in
        Prijavljeni ste kao administrator.
        | #{link_to "Odjava", logout_path, method: :delete}
        | #{link_to "Admin", admin_root_path}
