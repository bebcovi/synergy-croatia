- @title = t(".page_title")

%h1= @title

= markdown t(".body")

%ul.partner_list
  = list_of @partners do |partner|
    %a{href: partner.url, target: "_blank"}
      = image_tag(partner.photo_url(:small), alt: partner.name)
      %div= partner.name
